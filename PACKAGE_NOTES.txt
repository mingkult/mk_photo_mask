V4.4: removed all automatic backup, manual backup/recovery, load/save work-project (.mkpm), and work-project image snapshot format mechanisms. Keeps photo masking, AI/manual workflows, batch management, Canvas memory parking, Undo/Redo, cancellable operations, PNG/ZIP downloads, offline AI, GitHub build/release and Code Signing support. Current display/build version is 4.4.
