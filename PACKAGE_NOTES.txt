V4.6: follows the requested UI wording changes: upload hint is "點選本區域或直接拖曳到這裡", "已選照片" is now "選取區", and group-photo enhanced AI is off by default. Startup is faster because MediaPipe / FaceAPI scripts, AI models/WASM checks, and dlib helper health checks are no longer initialized automatically at page open; they load only on first use. V4.4 work-project and backup mechanisms remain removed. Current display/build version is 4.6.
