mk_photo_mask V3.5
==================

- 新增照片狀態篩選：全部 / 原圖 / AI / 待套用 / 已修改，並即時顯示各狀態數量。
- 上一張 / 下一張會依目前篩選結果移動。
- 每張縮圖新增批次選取核取方塊。
- 新增批次管理：全選目前、取消選取、重設選取、下載選取、關閉選取。
- 多張選取下載會自動打包 ZIP。
- 批次關閉若包含已修改或待套用照片會額外警告。
- 保留單張取消修改 / 關閉、Undo / Redo、AI＋手動同步框選與全部既有功能。
- localStorage 設定鍵改為穩定名稱，並相容讀取 V3.1 舊設定。
- Windows build 預設版本更新為 3.5。
