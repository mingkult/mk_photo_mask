Photo Privacy Tool | WSL single-file build v3.2
=================================================

Run build-wsl.cmd, or from WSL run:

  bash ./build_single_exe.sh

The script clears old output/*.exe files first and produces only:

  output/mk_photo_mask_v3.2.exe

The application UI remains Traditional Chinese; build/output filenames use ASCII only to prevent encoding problems.
